name: Update Changelog

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Erlaubt manuellen Start

jobs:
  changelog:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0  # Komplette Historie für Changelog
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Generate Changelog
        id: changelog
        uses: metcalfc/changelog-generator@v4.6.2
        with:
          myToken: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
      
      - name: Update CHANGELOG.md
        run: |
          echo "# Changelog" > CHANGELOG_AUTO.md
          echo "" >> CHANGELOG_AUTO.md
          echo "${{ steps.changelog.outputs.changelog }}" >> CHANGELOG_AUTO.md
      
      - name: Commit Changelog
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "docs: Update auto-generated changelog [skip ci]"
          file_pattern: CHANGELOG_AUTO.md
          commit_user_name: github-actions[bot]
          commit_user_email: github-actions[bot]@users.noreply.github.com
